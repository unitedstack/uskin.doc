```javascript
import { InputNumber } from 'uskin';

ReactDOM.render(
  <div>
    <InputNumber
      min={0}
      max={100}
      value={36}
      step={2}
      onChange={this.OnNumberChange}
    />
  </div>,
  mountNode);
```
