```javascript
import { InputNumber } from 'uskin';

ReactDOM.render(
  <div>
    <InputNumber
      min={0}
      max={10}
      value={3}
      step={2}
      disabled={true}
    />
  </div>,
  mountNode);
```
