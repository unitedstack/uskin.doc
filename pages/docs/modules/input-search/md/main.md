```javascript
import { InputSearch } from 'uskin';

ReactDOM.render(
  <div>
    <InputSearch
      width={300}
      type='light'
      placeholder='请输入搜索内容'
      onChange={this.onChange}
    />
  </div>,
  mountNode);
```
