```javascript
import {Button} from 'uskin';

ReactDOM.render(
  <div>
    <Button>default</Button>
    <Button slected={true}>slected</Button>
    <Button disabled={true}>disabled</Button>
  </div>,
  mountNode);
```
