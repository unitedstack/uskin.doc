```javascript
import {Button} from 'uskin';

ReactDOM.render(
  <div>
    <Button size="xm">xm</Button>
    <Button size="sm">sm</Button>
    <Button initial=true>initial</Button>
    <Button size="lg">lg</Button>
    <Button size="xl">xl</Button>
  </div>,
  mountNode);
```
